import ChangePasswordController from './ChangePasswordController';

const Settings = {
    ChangePasswordController: Object.assign(
        ChangePasswordController,
        ChangePasswordController,
    ),
};

export default Settings;
